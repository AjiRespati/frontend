import 'package:flutter/material.dart';

class StockDetailDesktop extends StatelessWidget {
  const StockDetailDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
