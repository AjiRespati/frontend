import 'package:flutter/material.dart';

class ClientDetailDesktop extends StatelessWidget {
  const ClientDetailDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
