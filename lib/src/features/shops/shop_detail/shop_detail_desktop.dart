import 'package:flutter/material.dart';

class ShopDetailDesktop extends StatelessWidget {
  const ShopDetailDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
