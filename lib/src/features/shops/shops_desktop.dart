import 'package:flutter/material.dart';

class ShopsDesktop extends StatelessWidget {
  const ShopsDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
