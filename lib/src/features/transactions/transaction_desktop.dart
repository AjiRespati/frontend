import 'package:flutter/material.dart';

class TransactionDesktop extends StatelessWidget {
  const TransactionDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
