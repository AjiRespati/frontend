import 'package:flutter/material.dart';

class PercentagesDesktop extends StatelessWidget {
  const PercentagesDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
