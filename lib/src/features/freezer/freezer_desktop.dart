import 'package:flutter/material.dart';

class FreezerDesktop extends StatelessWidget {
  const FreezerDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
